Practice Questions
1. Which of the following are operators, and which are values?

* (Operator)
'hello' (Value)
-88.8 (Value)
- (Operator)
/ (Operator)
+ (Operator)
5 (Value)

2. Which of the following is a variable, and which is a string?

spam (Variable)
'spam' (String

3. Name three data types. 
(integer, string, float)

4. What is an expression made up of? What do all expressions do?
(Value and Operator. evaluates the expression for a single new value)

5. This chapter introduced assignment statements, like spam = 10. What is the difference between an expression and a statement?
 (An expression is a combination of operators, values, variables and functions which always produces a final result or a value. A statement is an any executable unit code)
6. What does the variable bacon contain after the following code runs?

bacon = 20
bacon + 1
(The bacon value would be 20)
7. What should the following two expressions evaluate to?

'spam' + 'spamspam' (spamspamspam)
'spam' * 3 (spamspamspam)

8. Why is eggs a valid variable name while 100 is invalid?
(As 100 disobeys the basic rules of variable naming in the python language.)

9. What three functions can be used to get the integer, floating-point number, or string version of a value?

10. Why does this expression cause an error? How can you fix it?

'I have eaten ' + 99 + ' burritos.'
(As '+' operator can be used only to add two intergers together or to concatenate two strings.)

Extra credit: Search online for the Python documentation for the len() function. It will be on a web page titled “Built-in Functions.” Skim the list of other functions Python has, look up what the round() function does, and experiment with it in the interactive shell.